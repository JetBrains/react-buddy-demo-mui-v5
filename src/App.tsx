import * as React from 'react';
import {
  Box,
  Container,
  Divider,
  ListItemIcon,
  ListItemText,
  MenuItem,
  MenuList,
  SpeedDial, SpeedDialAction,
  SpeedDialIcon, Step, StepLabel, Stepper, Tab, Tabs, TextField,
  Typography
} from "@mui/material";
import {Cloud, ContentCopy, ContentCut, ContentPaste, Print, Save, Share} from "@mui/icons-material";

export default function App() {
  return (
    <Container maxWidth="sm">

    </Container>
  );
}
