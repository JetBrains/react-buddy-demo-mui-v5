import * as React from 'react';
import {
  Accordion,
  AccordionDetails,
  AccordionSummary,
  Alert,
  AppBar, Button, Card, CardActions, CardContent,
  Container, IconButton, Paper, SpeedDial, SpeedDialAction, SpeedDialIcon,
  Stack, Toolbar,
  Typography
} from "@mui/material";
import {ExpandMore, Menu, Print, Save, Share} from "@mui/icons-material";

export default function App() {
  return (
    <Container maxWidth="sm">

    </Container>
  );
}
